# Use straight-line geodesic distance

The app compares two GPS points, so the distance it reports is the straight-line geodesic distance between them rather than a route distance along roads or paths. That keeps the model simple, matches the capture flow, and avoids depending on map-routing data.
